version: 0.2 # 最新。https://docs.aws.amazon.com/ja_jp/codebuild/latest/userguide/build-spec-ref.html#build-spec-ref-versions

env:
  parameter-store:
    GITHUB_TOKEN: "/continuous_apply/github_token"

phases:
  build:
    commands:
      - ${CODEBUILD_SRC_DIR}/scripts/build.sh