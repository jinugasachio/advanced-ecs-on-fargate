name: Renovate

on: pull_request

permissions:
  id-token: write
  contents: read

jobs:  
  update-terraform:
    name: Terraform version update
    runs-on: ubuntu-latest
    if: startsWith(github.head_ref, 'renovate-terraform')
    steps: 
      - name: Checkout repository
        uses: actions/checkout@v2 # https://github.com/actions/checkout デフォルトで pull/xxx/merge ブランチにチェックアウトする

      - name: Detect terraform version
        run: printf "TF_VERSION=%s" $(cat .terraform-version) >> $GITHUB_ENV
      
      - name: Setup terraform
        uses: hashicorp/setup-terraform@v1 # https://github.com/hashicorp/setup-terraform
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Install tfnotify
        run: ./scripts/install.sh

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1 # https://github.com/aws-actions/configure-aws-credentials
        with:
          role-to-assume: arn:aws:iam::167855287371:role/yukio-ugajin-test-for-github-actions
          role-session-name: GitHubActions
          aws-region: us-west-2 # オレゴン

      - name: Confirm AWS credentials
        run: aws sts get-caller-identity

      # この後は.terraform.lock.hclがあるディレクトリ分だけinit & plan をする
      

      # PRのブランチにチェックアウト
      # - name: Checkout
      #   uses: actions/checkout@v2
      #   with:
      #     ref: ${{github.event.pull_request.head.ref}}
      # - name: confirm
      #   run: |
      #     git status
      #     git branch
  # update-terraform-provider
